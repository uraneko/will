mod setup;

use setup::init;

fn main() {
    // start the server
    // let conn = init();

    // eprintln!("{:?}", conn);

    // for stream in conn.incoming() {
    //     eprintln!("\r\n{:?}", stream);
    // }
}
